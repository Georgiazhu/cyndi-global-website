# 控制台使用说明（Admin Console）

Cyndi Global 后台控制台，用于管理 **Swag 商品** 和 **物流记录**。

## 一、进入与登录

| 环境 | 地址 |
|---|---|
| 线上 | `https://www.cyndiglobal.com/admin/` |
| 本地 | `http://localhost:8788/admin/`（需先 `npx wrangler pages dev --port 8788`）|

- 后台首页有两张卡片：**Products（商品管理）** 和 **Shipping（物流管理）**。
- 顶部导航栏在所有后台页面间通用：**Home / Products / Shipping**。
- 登录方式：**用户名 + 密码**。商品页和物流页**共用同一个登录会话**——登录一次，两个都能用，8 小时内免重复登录。
- 退出：任意页面点 **Log out / 退出登录**。

> 首次登录后请尽快修改默认密码（见文末“修改密码”）。

---

## 二、商品管理（Products）

地址：`/admin/products`

### 商品列表

- 分页显示（每页 20 / 50 / 100 可切换），底部翻页。
- 顶部可**按名称/SPU/品牌搜索**、**按状态筛选**（全部 / 上架 / 隐藏）。
- 每行显示：主图、SPU（可点击，新标签打开该商品的商城详情页 PDP）、名称、品牌、价格、状态、阶梯定价开关。

### 上架 / 下架

- 每行 **Hide / Show** 按钮。
- **Hide（隐藏）**：商品从商城前台消失（顾客看不到），后台仍可见。
- **Show（上架）**：恢复到商城前台。
- 改动**立即生效**（会清除前台缓存）。

### 阶梯定价开关（tiered）

- 每行的 **tiered** 复选框。
- **关闭（默认）**：商品用单一价。绝大多数商品用这个。
- **开启**：启用批发阶梯价（如“买 300 件更便宜”，主要针对 1688 货源），商城详情页会显示阶梯。
- 数据里的阶梯明细一直保留，开关只控制是否启用/显示。

### 改价格

1. 点某商品的 **Prices (N)** 按钮 → 该行下方展开**价格表**（按颜色 × 尺码列出每个 SKU）。
2. 直接在 **Price** 输入框改数字。
3. 点 **Save prices** 保存。
4. 保存后价格立即更新（前台缓存自动清除）。

> 每个 SKU（颜色+尺码）可单独定价。价格区间会自动按所有 SKU 的最低/最高重算。

### 编辑商品（名称/品牌/描述/币种/颜色/尺码/图片）

1. 点某商品的 **Edit** 按钮 → 下方展开完整编辑面板。
2. 可编辑：
   - **Name / Brand / Description**（名称、品牌、描述）
   - **Currency**（币种：USD / CNY）—— 改后商品和所有 SKU 一起更新，前台按币种显示 $ / ¥。
   - **Default image URL**（主图地址）
   - **颜色区块**（每个颜色一块，带左侧蓝边和标题“Color N”便于区分）：
     - Color name / Code（颜色名、颜色码；码留空自动生成 C01/C02…）
     - Swatch URL（色块图）
     - Image URLs（该颜色的图片，一行一个）
     - **Sizes**（尺码，逗号分隔，如 `S,M,L` 或 `OS`）
     - **+ Add color / Remove color**：增删颜色
3. 点 **Save changes** 保存。

> 增删颜色/尺码会重建对应 SKU，新增的 SKU **继承当前价格**（可再到 Prices 里单独调）。

### 上传商品图片

- 在编辑面板每个颜色区块下，有 **选择文件 + Upload image** 按钮。
- 选图 → 点 **Upload image** → 图片上传到 R2（`images/products/<SPU>/`），返回的公开 URL 自动追加到该颜色的 Image URLs（swatch 为空时自动填第一张）。
- 上传后记得点 **Save changes** 才会写入。
- 说明：上传依赖生产环境的 R2 绑定；本地开发环境上传走本地模拟，主要用于验证流程。

---

## 三、物流管理（Shipping）

地址：`/admin/logistics.html`

- 用**产品编号**（如 `CY-THERMOS-001`）作为查询/编辑键。
- 在“产品编号”框输入编号（输入 2 位以上会自动加载已有记录），或直接新建。
- 在 JSON 编辑区维护物流数据：运输状态、起止地、运输节点（events）、仓库位置、`warehouseImages` 等。除 `productNo`、`productName` 外可自由扩展字段（订单、货物、报关、运输、联系人、单证、备注）。
- **批量上传图片**：填产品编号 → 选图（一次最多 20 张）→ 上传，图片地址自动加入 `warehouseImages`，然后点**保存物流记录**。
- 每次保存生成一个历史版本，可加载查看/回溯。
- 单条记录建议控制在 1.9MB 内；图片走上传，不要塞进 JSON。

- 公开查询页：`/catalog/logistics.html`（顾客输入产品编号查物流状态、轨迹、仓库照片）。

> 物流的产品编号与商城的 SPU 是同一套 `CY-XXX-NNNN`：商城商品的 SPU 可直接作为物流 productNo 维护物流信息。

---

## 四、常见操作对照

| 想做的事 | 去哪 |
|---|---|
| 改某商品价格 | Products → Prices → 改 → Save prices |
| 下架/上架商品 | Products → 该行 Hide / Show |
| 改名称/描述/颜色/尺码 | Products → Edit → 改 → Save changes |
| 传商品图 | Products → Edit → 颜色区块 → Upload image → Save changes |
| 开启批发阶梯价 | Products → 该行勾选 tiered |
| 看商品前台效果 | Products → 点该行 SPU（新标签打开 PDP）|
| 维护物流轨迹 | Shipping → 输产品编号 → 编辑 JSON → 保存 |

---

## 五、账户与安全

### 修改密码

- 物流后台页面底部有“修改登录密码”（Change login password）区，输入当前密码 + 新密码（8–128 位）即可修改。修改后需重新登录。
- 该密码是后台通用登录密码（商品和物流共用）。

### 多管理员

- 管理员账户存在 D1 `admin_users` 表（用户名 + 加盐哈希）。新增管理员目前通过数据库添加。
- 一旦 `admin_users` 表里有账号，登录以用户名为准；旧的单一密码仅作无账号时的兜底。

### 安全建议

- 上线后**尽快把默认密码改成强密码**。
- 后台所有写操作都需登录会话；未登录调用会返回 401。
