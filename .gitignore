.env
.env.*
.dev.vars
.dev.vars.*
.wrangler/
node_modules/
dist/
*.log
*.pem
*.key

# D1 database backups (contain admin password hash + business data — never commit)
*-backup.sql
cyndi-logistics-backup.sql

# macOS
.DS_Store

# Python virtualenv for crawler
.crawl-venv/

# Crawler outputs (raw HTML, images, third-party product data — regenerable, not for VCS)
scripts/output/

# Service showcase images are hosted on R2 (img.cyndiglobal.com), not committed.
# Keep the manifest JSONs; ignore the image binaries.
product-showcase/public/images/services/**/*.jpg
product-showcase/public/images/services/**/*.jpeg
product-showcase/public/images/services/**/*.png

# Local session/handoff notes and scratch (not for VCS)
.kiro/
HANDOFF.md
_p.html
__pycache__/
